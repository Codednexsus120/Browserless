name: SSHX Runner

on:
  workflow_dispatch:   # manually trigger

jobs:
  sshx:
    runs-on: ubuntu-latest
    steps:
      - name: Run SSHX
        run: curl -sSf https://sshx.io/get | sh -s run
        if: always()
